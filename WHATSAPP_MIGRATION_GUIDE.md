# Краткая инструкция по миграции с Prelude на WhatsApp

## ✅ Выполненные изменения

### 1. Код (authController.ts)
- ❌ Удалён импорт `@prelude.so/sdk`
- ✅ Добавлен импорт `axios`
- ✅ Создана функция `sendCodeViaWhatsApp()` для отправки через WhatsApp
- ✅ Создана функция `generateVerificationCode()` для генерации 6-значного кода
- ✅ Обновлён метод `sendCode()`:
  - Генерация собственного кода
  - Хеширование кода (bcrypt)
  - Сохранение в БД
  - Отправка через WhatsApp
  - Rate limiting (1 мин)
- ✅ Обновлён метод `verifyCode()`:
  - Проверка кода из БД
  - Проверка срока действия (10 мин)
  - Валидация формата

### 2. Зависимости (package.json)
- ❌ Удалена зависимость `@prelude.so/sdk`
- ✅ Используется существующая зависимость `axios`

### 3. Конфигурация (.env.example)
- ✅ Добавлена переменная `WHATSAPP_ACCESS_TOKEN`
- ✅ Добавлена переменная `WHATSAPP_PHONE_NUMBER_ID`

### 4. Документация
- ✅ Создан файл `WHATSAPP_AUTH_SETUP.md` с полной инструкцией

---

## 🚀 Что нужно сделать для запуска

### 1. Настроить WhatsApp Business (5-10 минут)

1. **Перейдите на** https://business.facebook.com/
2. **Создайте Business Account** (если нет)
3. **Добавьте приложение WhatsApp Business**
4. **Создайте шаблон сообщения**:
   - Название: `access`
   - Категория: Authentication
   - Язык: Russian (ru)
   - Текст: `Ваш код подтверждения: {{1}}\n\nНикому не сообщайте этот код.`
   - **Отправьте на одобрение** (обычно одобряется за несколько часов)

### 2. Получить учетные данные (2 минуты)

1. В **WhatsApp Manager → API Setup**:
   - Скопируйте **Phone Number ID** (например: `958088394044701`)
   - Сгенерируйте **Access Token** (постоянный токен)

### 3. Настроить переменные окружения (1 минута)

Создайте файл `.env` в корне проекта:

```env
# WhatsApp Business API
WHATSAPP_ACCESS_TOKEN=EAAxxxxxxxxxxxxxxxxxxxxxxxxxx
WHATSAPP_PHONE_NUMBER_ID=958088394044701

# Остальные переменные (если есть)
JWT_SECRET=your-secret-key
DATABASE_URL=your-database-url
```

⚠️ **Важно**: Не коммитьте `.env` в git!

### 4. Запустить сервер (1 минута)

```bash
# Установить зависимости (если ещё не установлены)
npm install

# Запустить в режиме разработки
npm run dev
```

---

## 🧪 Тестирование (3 минуты)

### 1. Добавить тестовый номер

В WhatsApp Manager → API Setup → "Add test numbers"
Добавьте свой номер WhatsApp для тестирования.

### 2. Отправить код

```bash
curl -X POST "http://localhost:3000/api/auth/send-code" \
  -H "Content-Type: application/json" \
  -d '{"phone_number": "+77054066676"}'
```

### 3. Проверить WhatsApp

Откройте WhatsApp на телефоне - должно прийти сообщение с кодом.

### 4. Верифицировать код

```bash
curl -X POST "http://localhost:3000/api/auth/verify-code" \
  -H "Content-Type: application/json" \
  -d '{"phone_number": "+77054066676", "onetime_code": "123456"}'
```

Где `123456` - код, который пришел в WhatsApp.

---

## 📋 Checklist перед деплоем

- [ ] WhatsApp Business Account создан
- [ ] Шаблон `access` одобрен Meta
- [ ] Phone Number ID получен
- [ ] Access Token сгенерирован
- [ ] Переменные окружения настроены в production
- [ ] Тестирование пройдено успешно
- [ ] HTTPS настроен для production
- [ ] Мониторинг логов настроен

---

## 🔍 Проверка статуса

### Проверить компиляцию:
```bash
npx tsc --noEmit
```

### Проверить зависимости:
```bash
npm list @prelude.so/sdk  # Должно показать "not found"
npm list axios            # Должно показать установленную версию
```

### Проверить переменные окружения:
```bash
node -e "require('dotenv').config(); console.log(process.env.WHATSAPP_ACCESS_TOKEN ? 'OK' : 'NOT SET')"
```

---

## 🐛 Частые проблемы

### "Template not found"
**Решение**: Шаблон должен быть одобрен Meta перед использованием

### "WHATSAPP_ACCESS_TOKEN не установлен"
**Решение**: Добавьте переменную в `.env` файл

### Код не приходит
**Проверьте**:
1. Номер в тестовых номерах (для dev)
2. Шаблон одобрен
3. Phone Number ID корректный
4. Access Token действителен

---

## 📞 Поддержка

- **Полная документация**: `WHATSAPP_AUTH_SETUP.md`
- **WhatsApp Docs**: https://developers.facebook.com/docs/whatsapp
- **Meta Business Suite**: https://business.facebook.com/

---

**Статус**: ✅ Миграция завершена, код готов к запуску после настройки WhatsApp Business
