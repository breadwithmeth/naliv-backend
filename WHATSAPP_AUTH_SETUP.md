# Настройка WhatsApp Business API для авторизации

## 📋 Обзор изменений

Система авторизации переведена с **Prelude SDK** на собственную реализацию с отправкой кодов через **WhatsApp Business API**.

### Преимущества:
- ✅ Полный контроль над процессом верификации
- ✅ Хранение кодов в собственной БД
- ✅ Настройка срока действия кода (10 минут)
- ✅ Защита от спама (1 запрос в минуту)
- ✅ Хеширование кодов в БД
- ✅ Отправка через WhatsApp (более высокая доставляемость)

---

## 🚀 Быстрый старт

### 1. Получение доступа к WhatsApp Business API

#### Шаг 1: Создать Meta Business аккаунт
1. Перейдите на https://business.facebook.com/
2. Создайте новый Business Account или используйте существующий

#### Шаг 2: Настроить WhatsApp Business
1. В Meta Business Suite перейдите в раздел "WhatsApp"
2. Создайте новое приложение или используйте существующее
3. Добавьте продукт "WhatsApp Business API"

#### Шаг 3: Получить учетные данные
1. Перейдите в настройки приложения WhatsApp
2. Скопируйте **Phone Number ID** (например: `958088394044701`)
3. Сгенерируйте **Access Token** (долгосрочный токен)

#### Шаг 4: Создать шаблон сообщения
В WhatsApp Manager создайте шаблон с именем `access`:

**Название**: `access`
**Категория**: Authentication (Аутентификация)
**Язык**: Russian (ru)

**Текст шаблона**:
```
Ваш код подтверждения: {{1}}

Никому не сообщайте этот код.
```

**Кнопки**: Нет

⚠️ **Важно**: Шаблон должен быть одобрен Meta перед использованием.

---

## ⚙️ Настройка переменных окружения

Создайте файл `.env` в корне проекта (если ещё не создан):

```bash
cp .env.example .env
```

Добавьте следующие переменные:

```env
# WhatsApp Business API Configuration
WHATSAPP_ACCESS_TOKEN=EAAxxxxxxxxxxxxxxxxxxxxxxxxxx
WHATSAPP_PHONE_NUMBER_ID=958088394044701
```

### Где найти значения:

- **WHATSAPP_ACCESS_TOKEN**: Meta Business Suite → WhatsApp → API Setup → Access Token
- **WHATSAPP_PHONE_NUMBER_ID**: Meta Business Suite → WhatsApp → API Setup → Phone Number ID

---

## 📝 Использование API

### 1. Отправка кода
**POST** `/api/auth/send-code`

```bash
curl -X POST "http://localhost:3000/api/auth/send-code" \
  -H "Content-Type: application/json" \
  -d '{
    "phone_number": "+77054066676"
  }'
```

**Ответ:**
```json
{
  "success": true,
  "data": {
    "phone_number": "+77054066676",
    "message": "Код отправлен в WhatsApp на указанный номер"
  },
  "message": "Код отправлен"
}
```

### 2. Верификация кода
**POST** `/api/auth/verify-code`

```bash
curl -X POST "http://localhost:3000/api/auth/verify-code" \
  -H "Content-Type: application/json" \
  -d '{
    "phone_number": "+77054066676",
    "onetime_code": "123456"
  }'
```

**Ответ:**
```json
{
  "success": true,
  "data": {
    "user": {
      "user_id": 1,
      "name": null,
      "login": "+77054066676",
      "log_timestamp": "2024-12-10T10:00:00.000Z"
    },
    "password": "a1b2c3d4e5f6",
    "token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
    "session_token": "1702210800000-abc123-1"
  },
  "message": "Авторизация успешна"
}
```

---

## 🔐 Безопасность

### Реализованные меры:

1. **Хеширование кодов**: Коды хранятся в БД в хешированном виде (bcrypt)
2. **Срок действия**: Код действителен 10 минут
3. **Rate limiting**: Максимум 1 запрос в минуту на номер
4. **Одноразовое использование**: Код помечается как использованный после проверки
5. **Валидация формата**: Проверка формата номера телефона и кода

### Рекомендации:

- ✅ Используйте HTTPS в production
- ✅ Храните WHATSAPP_ACCESS_TOKEN в секретах (не коммитьте в git)
- ✅ Ротируйте токены регулярно
- ✅ Настройте мониторинг WhatsApp API
- ✅ Добавьте логирование попыток авторизации

---

## 🗄️ База данных

### Таблица phone_number_verify

Коды хранятся в таблице `phone_number_verify`:

```sql
CREATE TABLE phone_number_verify (
  verification_id INT PRIMARY KEY AUTO_INCREMENT,
  phone_number VARCHAR(20) NOT NULL,
  onetime_code VARCHAR(255) NOT NULL,  -- Хешированный код
  is_used TINYINT DEFAULT 0,
  log_timestamp TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

**Поля:**
- `phone_number` - номер телефона в формате +77077707600
- `onetime_code` - хешированный код верификации (bcrypt)
- `is_used` - флаг использования (0 - не использован, 1 - использован)
- `log_timestamp` - время создания записи

---

## 🧪 Тестирование

### Тестовый режим WhatsApp

Meta предоставляет тестовые номера для разработки:

1. Перейдите в WhatsApp Business Platform → API Setup
2. Добавьте тестовые номера телефонов
3. Используйте эти номера для тестирования

### Локальное тестирование

```bash
# 1. Запустите сервер
npm run dev

# 2. Отправьте код
curl -X POST "http://localhost:3000/api/auth/send-code" \
  -H "Content-Type: application/json" \
  -d '{"phone_number": "+77054066676"}'

# 3. Проверьте WhatsApp на тестовом номере

# 4. Введите полученный код
curl -X POST "http://localhost:3000/api/auth/verify-code" \
  -H "Content-Type: application/json" \
  -d '{"phone_number": "+77054066676", "onetime_code": "123456"}'
```

---

## 🐛 Устранение неполадок

### Ошибка: "WHATSAPP_ACCESS_TOKEN не установлен"

**Решение**: Добавьте переменную в `.env`:
```env
WHATSAPP_ACCESS_TOKEN=your_token_here
```

### Ошибка: "Template not found"

**Решение**: 
1. Создайте шаблон `access` в WhatsApp Manager
2. Дождитесь одобрения шаблона Meta
3. Убедитесь, что имя шаблона точно `access`

### Ошибка: "Invalid phone number"

**Решение**: 
- Используйте формат +77XXXXXXXXXX (11 цифр после +7)
- Номер должен быть зарегистрирован в WhatsApp

### Сообщение не приходит

**Проверьте**:
1. Номер добавлен в тестовые номера (для разработки)
2. Шаблон одобрен Meta
3. Phone Number ID корректный
4. Access Token действителен
5. Логи сервера на наличие ошибок

---

## 📊 Мониторинг

### Логи WhatsApp API

Все запросы к WhatsApp API логируются:

```typescript
console.log('WhatsApp message sent:', response.data);
console.error('WhatsApp send error:', err.response?.data || err.message);
```

### Метрики для отслеживания:

- Количество отправленных кодов
- Успешность доставки (WhatsApp API response)
- Количество успешных верификаций
- Время между отправкой и верификацией
- Процент истекших кодов

---

## 🔄 Миграция с Prelude

### Что изменилось:

| Аспект | Prelude | Новая система |
|--------|---------|---------------|
| Генерация кода | Prelude | Собственная (6 цифр) |
| Хранение | Не хранится | Хеш в БД |
| Проверка | Prelude API | Bcrypt.compare |
| Канал | SMS | WhatsApp |
| Срок действия | Prelude настройки | 10 минут |
| Rate limit | Prelude | 1 мин между запросами |

### Удалено:

- ❌ `@prelude.so/sdk` зависимость
- ❌ `sendSMSViaPrelude()` функция
- ❌ `checkVerificationCodeViaPrelude()` функция
- ❌ Hardcoded Prelude API ключ

### Добавлено:

- ✅ `sendCodeViaWhatsApp()` функция
- ✅ `generateVerificationCode()` функция
- ✅ Хеширование кодов
- ✅ Rate limiting
- ✅ Проверка срока действия

---

## 📚 Дополнительные ресурсы

- [WhatsApp Business Platform Documentation](https://developers.facebook.com/docs/whatsapp)
- [Meta Business Suite](https://business.facebook.com/)
- [WhatsApp Template Guidelines](https://developers.facebook.com/docs/whatsapp/message-templates/guidelines)
- [WhatsApp API Reference](https://developers.facebook.com/docs/whatsapp/cloud-api/reference)

---

## 📞 Поддержка

Если возникли проблемы:

1. Проверьте логи сервера
2. Убедитесь в корректности настроек WhatsApp
3. Проверьте статус одобрения шаблона
4. Обратитесь к Meta Developer Support

---

**Статус**: ✅ **ГОТОВО К PRODUCTION** (после настройки WhatsApp Business Account)
